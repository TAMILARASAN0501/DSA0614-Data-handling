# Data
weight <- c(60, 65, 70, 75, 80, 85, 90, 95, 100)

# Density plot
plot(density(weight), 
     main="Density Plot of Weight",
     xlab="Weight (kg)", 
     ylab="Density",
     col="blue")